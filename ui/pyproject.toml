[project]
name = "medcopilot-ui"
version = "0.1.0"
description = "Streamlit demo UI for MedCoPilot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.40.0",
    "requests>=2.32.0",
]
