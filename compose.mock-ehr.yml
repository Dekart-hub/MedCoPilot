services:
  mock-ehr:
    image: hapiproject/hapi:v8.10.0-2
    ports:
      - "${MOCK_EHR_PORT:-8080}:8080"
    environment:
      hapi.fhir.fhir_version: R4
      hapi.fhir.default_encoding: json
      hapi.fhir.allow_external_references: "false"
